{
  "name": "seedling",
  "version": "0.1.0",
  "description": "From scattered grants to funded missions — in one place.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\" --names \"client,server\" --prefix-colors \"green,blue\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
